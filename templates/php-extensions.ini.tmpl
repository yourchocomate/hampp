; Generated by hampp: extensions enabled with `hampp php ext enable`.
; Termux installs some extension packages (e.g. php-redis, php-apcu) without an
; ini file, so they stay off until enabled here.
{{range .PHPExtensions}}extension={{.}}
{{end}}